var baseFact = require('./baseFact');

class locationUpdateFact extends baseFact {
    make() {
        return "In the icey, cold and magical North Pole";
    }
};

module.exports = locationUpdateFact;